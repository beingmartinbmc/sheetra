# Local Benchmark Files

Place large local CSV/XLSX benchmark fixtures in this directory.

The repository ignores `*.csv` files here because realistic fixtures can be hundreds of megabytes and GitHub rejects files over 100MB. The benchmark scripts discover files from this folder at runtime.
